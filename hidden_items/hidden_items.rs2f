
// module:item_values


#define HIDE_1
#define HIDE_5
#define HIDE_40
#define HIDE_60

if (name:HIDE_1 && quantity:<2) { hidden = true; }
if (name:HIDE_5 && quantity:<6) { hidden = true; }
if (name:HIDE_40 && quantity:<41) { hidden = true; }
if (name:HIDE_60 && quantity:<61) { hidden = true; }

// endmodule:item_values